name: Deploy to Remote Server

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v3
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository_owner | lower }}/${{ github.event.repository.name | lower }}:latest

      - name: Display Last Three Characters of Secrets
        run: |
          echo "DEPLOY_HOST last 3 chars: ${${{ secrets.DEPLOY_HOST }}: -3}"
          echo "DEPLOY_USERNAME last 3 chars: ${${{ secrets.DEPLOY_USERNAME }}: -3}"
          echo "DEPLOY_KEY last 3 chars: ${${{ secrets.DEPLOY_KEY }}: -3}"
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_USERNAME: ${{ secrets.DEPLOY_USERNAME }}
          DEPLOY_KEY: ${{ secrets.DEPLOY_KEY }}

      - name: Copy files to remote server
        uses: appleboy/scp-action@v0.1.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USERNAME }}
          key: ${{ secrets.DEPLOY_KEY }}
          source: "."
          target: "~/apps/porteFollio"

      - name: Deploy application via SSH
        uses: appleboy/ssh-action@v0.1.5
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USERNAME }}
          key: ${{ secrets.DEPLOY_KEY }}
          script: |
            cd ~/apps/porteFollio
            docker build -t porte-follio .
            docker run -d -p 80:80 porte-follio
